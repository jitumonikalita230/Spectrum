<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Animated Profile Card (HTML + CSS)</title>
  <style>
    :root{
      --bg:#0f1724;
      --card:#0b1220;
      --accent:#06b6d4;
      --glass: rgba(255,255,255,0.04);
      --text:#e6eef8;
      --muted: rgba(230,238,248,0.7);
    }

    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      display:grid;
      place-items:center;
      font-family:Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: radial-gradient(1200px 600px at 10% 20%, rgba(6,182,212,0.08), transparent),
                  radial-gradient(800px 400px at 90% 80%, rgba(59,130,246,0.04), transparent),
                  var(--bg);
      color:var(--text);
      padding:40px;
    }

    .profile-card{
      width:320px;
      max-width:94vw;
      perspective:1200px;
    }

    .tog{display:none;}

    .card-inner{
      width:100%;
      position:relative;
      transform-style:preserve-3d;
      transition: transform 700ms cubic-bezier(.2,.8,.2,1);
      border-radius:18px;
      box-shadow: 0 10px 30px rgba(2,6,23,0.6), inset 0 1px 0 rgba(255,255,255,0.02);
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      overflow:hidden;
      user-select:none;
    }

    .profile-card:hover .card-inner,
    .tog:checked + .card-inner{
      transform: rotateY(180deg);
    }

    .card-face{
      position: relative;
      min-height: 420px;
    }

    /* ✅ FIXED FLIP LAYERING */
    .front, .back {
      position: absolute;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      backface-visibility: hidden;
    }

    .front{
      background: linear-gradient(180deg,var(--card), rgba(8,12,18,0.7));
      display:flex;
      flex-direction:column;
      align-items:center;
      padding:26px 20px;
      gap:18px;
    }

    .avatar-wrap{
      width:160px;
      height:160px;
      border-radius:50%;
      padding:6px;
      background: linear-gradient(135deg, rgba(6,182,212,0.12), rgba(99,102,241,0.08));
      display:grid;
      place-items:center;
      transform: translateZ(40px) rotateZ(-2deg);
      transition: transform 600ms ease;
      box-shadow: 0 8px 30px rgba(2,6,23,0.6);
    }

    .avatar-wrap img{
      width:100%;
      height:100%;
      object-fit:cover;
      border-radius:50%;
      display:block;
      border:4px solid rgba(255,255,255,0.06);
    }

    .name{
      font-size:20px;
      font-weight:700;
      letter-spacing:0.2px;
      margin:0;
      color:var(--text);
    }

    .role{
      color:var(--muted);
      font-size:13px;
      margin:0;
    }

    .bio{
      font-size:13px;
      color:var(--muted);
      text-align:center;
      line-height:1.45;
      padding:0 12px;
    }

    .badges{
      display:flex;
      gap:10px;
      margin-top:6px;
    }

    .badge{
      background:var(--glass);
      padding:6px 10px;
      border-radius:999px;
      font-size:12px;
      color:var(--muted);
    }

    .back{
      transform:rotateY(180deg);
      background: linear-gradient(180deg, rgba(6,182,212,0.02), rgba(99,102,241,0.01));
      padding:22px;
      display:flex;
      flex-direction:column;
      gap:12px;
      justify-content:center;
    }

    .info-row{
      display:flex;
      justify-content:space-between;
      align-items:center;
    }

    .label{font-size:13px;color:var(--muted)}
    .value{font-weight:600}

    .socials{
      display:flex;
      gap:12px;
      padding-top:8px;
      flex-wrap:wrap;
    }

    .social{
      flex:1 1 46%;
      background:linear-gradient(180deg, rgba(255,255,255,0.012), rgba(255,255,255,0.01));
      padding:10px 12px;
      border-radius:10px;
      font-size:13px;
      display:flex;
      align-items:center;
      gap:10px;
    }

    .callout{
      margin-top:8px;
      padding:10px;
      border-radius:10px;
      background:linear-gradient(90deg, rgba(6,182,212,0.06), rgba(99,102,241,0.04));
      color:var(--text);
      font-size:13px;
      text-align:center;
      font-weight:600;
    }

    .card-inner::before{
      content:"";
      position:absolute;
      inset:0;
      pointer-events:none;
      background:linear-gradient(120deg, transparent 0%, rgba(255,255,255,0.02) 40%, transparent 60%);
      mix-blend-mode:overlay;
    }

    .profile-card:hover .avatar-wrap{
      transform: translateZ(60px) rotateZ(-2deg) scale(1.03);
    }

    .profile-card:hover .name{
      transform: translateY(-2px);
    }

    @media (min-width:900px){
      .profile-card{ width:360px }
    }

    .hint{
      position:absolute;
      right:12px;
      bottom:12px;
      font-size:12px;
      color:var(--muted);
      opacity:0.9;
    }

  </style>
</head>
<body>

  <label class="profile-card">
    <input class="tog" type="checkbox" aria-hidden="true" />

    <div class="card-inner">

      <!-- FRONT -->
      <div class="card-face front">
        <div class="avatar-wrap">
          <img src="your-photo.jpg" alt="Your photo">
        </div>
        <h3 class="name">Your Name</h3>
        <p class="role">Your Role — e.g., Web Developer</p>
        <p class="bio">A short 1–2 line bio about you. Keep it punchy and personal!</p>

        <div class="badges">
          <span class="badge">HTML</span>
          <span class="badge">CSS</span>
          <span class="badge">Design</span>
        </div>

        <div class="hint">Hover or tap to view details →</div>
      </div>

      <!-- BACK -->
      <div class="card-face back">
        <div class="info-row"><div class="label">Email</div><div class="value">you@example.com</div></div>
        <div class="info-row"><div class="label">Location</div><div class="value">City, Country</div></div>
        <div class="info-row"><div class="label">Website</div><div class="value">your-website.com</div></div>

        <div class="socials">
          <div class="social">@yourhandle<br><small class="label">Twitter</small></div>
          <div class="social">github.com/you<br><small class="label">GitHub</small></div>
          <div class="social">linkedin.com/in/you<br><small class="label">LinkedIn</small></div>
          <div class="social">dribbble.com/you<br><small class="label">Dribbble</small></div>
        </div>

        <div class="callout">Open to work • Available for freelance</div>
      </div>

    </div>
  </label>

</body>
</html>
